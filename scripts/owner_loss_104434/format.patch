diff --git a/tools/async_delegation.py b/tools/async_delegation.py
--- a/tools/async_delegation.py
+++ b/tools/async_delegation.py
@@ -402,7 +402,12 @@ def get_event_delivery_claim_status(
             "SELECT delivery_state, delivery_claim FROM async_delegations "
             "WHERE delegation_id=?", (delegation_id,),
         ).fetchone()
-    return (None, False) if row is None else (row[0], bool(claim_id and row[0] == "pending" and row[1] == claim_id))
+    if row is None:
+        return None, False
+    return row[0], bool(
+        claim_id
+        and row[0] == "pending"
+        and row[1] == claim_id
+    )
